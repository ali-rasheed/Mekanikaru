class Module {
  
  int xPos, yPos, modW, modH;
  
  Module(int x, int y, int w, int h) {
    xPos = x;
    yPos = y;
    modW = w;
    modH = h;
  }
  
  void render() {
    //fill(255);
    noStroke();
    //rect(xPos,yPos,modW,modH);
  }
  
}
